# Copy to .env when backend (Phase V2) is used. Never commit real secrets.
# Core offline safety pipeline must NOT depend on any value here.
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BOUNDARY_DATA_VERSION=0.1.0-demo
MODEL_VERSION=0.1.0-untrained
